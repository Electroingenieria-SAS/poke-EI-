{
  "name": "echoes-of-alder-phaser-rpg",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "qa:assets": "node tools/validate-runtime-assets.mjs",
    "qa": "npm run qa:assets && npm run check && npm run build"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "typescript": "5.7.3",
    "vite": "6.1.0"
  }
}
